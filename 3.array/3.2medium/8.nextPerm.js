const brute = (arr) => {
  // sort the array
  // find all permutaion
  // check current array position in all permutaion
  // return the next array present right after the current array
};
arr = [1, 3, 2];
brute(arr);
